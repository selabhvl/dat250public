package no.hvl.dat250.l03;


/**
 * In this challenge, we will deal with POJOs!
 * POJO is an abbreviation that stands for Plain Old Java Object and was created as term by software engineering
 * professional as the opposite of (Enterprise) Java Beans. The latter often impose several requirements
 * on your classes, such as parameterless public constructor, public getters/setters for all properites and so on,
 * which in turn put limitations on how you can design your domain classes, e.g. you cannot have final attributes etc.
 * Also, beans have a tendency to lead to _anemic domain models_, an antipattern that you want to avoid.
 * Previously all the domain objects you have dealt with in previous challenges were beans and thus easy to (de-)serialize
 * via binding, but in this challenge you have deal with a Person class that is proper POJO.
 * You will have to find out how to use the Builder pattern and how getters can be used derive new attributes.
 */
public class Challenge3 {
}
